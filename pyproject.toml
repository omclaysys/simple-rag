[project]
name = "simple-rag"
version = "0.1.0"
description = "Simple RAG demo: chunk, embed with ChromaDB, retrieve top 3"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "chromadb",
]

[dependency-groups]
dev = [
    "ruff>=0.15.17",
]
